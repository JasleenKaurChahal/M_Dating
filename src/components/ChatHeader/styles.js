import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  head: {
    height: 60,
    backgroundColor: '#F7C505',
    padding: 20,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  image: {height: 20, width: 30},
  heading: {fontSize: 16, fontWeight: 'bold'},
});

export default styles;
